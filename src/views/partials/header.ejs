<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>ClickBeer</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="/css/estilos.css" /> 
</head>
<!-- jQuery CDN -->
<script
src="https://code.jquery.com/jquery-3.1.1.min.js"
integrity="sha256-hVVnYaiADRTO2PzUGmuLJr8BLUSjGIZsDYGmIJLv2b8="
crossorigin="anonymous">
</script>

<body>
    <nav class="navbar navbar-inverse navbar-static-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>                    
            </div>
            <div id="navbar" class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    <a href="/" class="navbar-left"><img class="logo" src="/imgs/site_logo_3.png"></a>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <% if(!usuarioAtual){ %>
                        <li class="<%= typeof page !== 'undefined' && page === 'Entrar' ? 'active' : '' %>"><a href="/users/login"><i class="glyphicon glyphicon-user"></i> Entrar</a></li>
                        <li class="<%= typeof page !== 'undefined' && page === 'Registrar' ? 'active' : '' %>"><a href="/users"><i class="glyphicon glyphicon-user"></i> Registrar</a></li>
                    <% } else { %>
                        <li><a href="/users/me"><i class="glyphicon glyphicon-user"></i> <%= usuarioAtual.username %></a></li>
                        <li><a href="/carrinho"><i class="glyphicon glyphicon-shopping-cart"></i> Carrinho de Compras</a></li>
                        <li><a href="/users/logout"><i class="glyphicon glyphicon-log-out"></i> Sair</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container alertas">
        <% if(error.length > 0) { %>
            <div class="alert alert-danger" id="error-alert" role="alert">
                <h1><%= error %></h1>
            </div>
        <% } %>
        <% if(success.length > 0) { %> 
            <div class="alert alert-success" id="success-alert" role="alert">
                <h1><%= success %></h1>
            </div>
        <% } %>
    </div>

    <div class="container">